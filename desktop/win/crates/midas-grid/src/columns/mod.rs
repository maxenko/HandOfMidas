//! Pre-built column types.
//!
//! Phase 0 ships empty — the watchlist uses a single `WatchlistColumn`
//! enum implementing `GridColumn`. Pre-built types (TextColumn,
//! NumericColumn, etc.) will be added in Phase 2+ when a second grid
//! consumer exists.
