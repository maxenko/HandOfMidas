# ── Rust build artifacts ────────────────────────────────────────────
**/target/
**/*.rs.bk

# ── Cargo lock — included for binaries, but this is a workspace ────
# Cargo.lock is committed (workspace with a future binary crate)

# ── IDE / editor ───────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
*.iml

# ── OS ─────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
.DS_Store?
._*

# ── Environment / secrets ──────────────────────────────────────────
.env
.env.*
*.pem
*.key
credentials.json

# ── Runtime data ───────────────────────────────────────────────────
**/data/
*.candles

# ── SQLite databases ──────────────────────────────────────────────
*.db
*.db-wal
*.db-shm

# ── DuckDB databases ─────────────────────────────────────────────
*.duckdb
*.duckdb.wal

# ── Build spikes (throwaway crates) ──────────────────────────────
desktop/win/duckdb-spike/

# ── Benchmark / profiling output ───────────────────────────────────
**/criterion/
perf.data
perf.data.old
flamegraph.svg

# ── Claude Code ────────────────────────────────────────────────────
.claude/

# ── IB-sim session captures ────────────────────────────────────────
# Raw (un-anonymized) wire captures. See plan/ib-sim/07-session-recording.md
# §"Data Lifecycle" for the policy. These MUST NEVER be committed.
crates/midas-ib-sim/fixtures/sessions/raw/
crates/midas-ib-sim/fixtures/sessions/**/raw/
